#!/usr/bin/env python3
"""Run without starting Odysseus or reading its live database."""
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from src.local_worker.cli import entry
raise SystemExit(entry())
